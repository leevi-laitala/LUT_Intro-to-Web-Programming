//function vectorDotProduct(ax, ay, bx, by)
//{
//    return ax * bx + ay * by
//}
//
//function vectorMagnitude(ax, ay)
//{
//    return Math.sqrt(ax * ax + ay * ay)
//}
//
//function vectorAngle(ax, ay, bx, by)
//{
//    let div = vectorDotProduct(ax, ay, bx, by) / (vectorMagnitude(ax, ay) * vectorMagnitude(bx, by))
//    return Math.acos(div)
//}
//
//function normalizeAngle(angle)
//{
//    angle += (angle < 0) ? Math.PI * 2 : 0
//    angle += Math.PI
//    angle = angle % (Math.PI * 2)
//    return angle
//}

